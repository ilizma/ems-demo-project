<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@+id/dashboard_screen">

    <fragment
        android:id="@+id/dashboard_screen"
        android:name="com.ilizma.dashboard.view.fragment.DashboardScreenFragment"
        tools:layout="@layout/dashboard_screen_fragment">

       <!-- TODO add navigation
       <action
            android:id="@+id/go_from_dashboard_to_detail"
            app:destination="@id/detail_screen" />
    </fragment>

    <fragment
        android:id="@+id/detail_screen"
        android:name="com.ilizma.schedule.view.fragment.ScheduleDetailFragment"
        tools:layout="@layout/schedule_detail_fragment">

        <argument
            android:name="type"
            app:argType="com.ilizma.schedule.flow.model.Type" /> -->
    </fragment>

</navigation>